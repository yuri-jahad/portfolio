import { css } from '~styled-system/css'

export const ContainerCSS = css({
  minHeight: '100vh',
  bg: 'bg.primary',
  color: 'text.primary'
})

export const SceneCSS = css({
  maxWidth: '80vw',
  margin: '0 auto',
//   bg: 'black',
  minHeight: '100vh'
})
